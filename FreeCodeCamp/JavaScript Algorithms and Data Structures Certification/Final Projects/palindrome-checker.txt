function palindrome(str) {
	// Good luck!

	var replaced = str.toLowerCase().replace(/[^a-zA-Z0-9]/g, "")
	var reversed = replaced.split("").reverse().join("");
	if (replaced === reversed) {
		return true;
	} else {
		return false;
	}

}

palindrome("eye");
