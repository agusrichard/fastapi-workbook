function findLongestWordLength(str) {
  let regex = /(\w+)/g;
  let list = str.match(regex);
  let nums = [];
  for (let i = 0; i < list.length; i++) {
  	nums.push(list[i].length);
  }
  return Math.max(...nums);
}

findLongestWordLength("The quick brown fox jumped over the lazy dog");
